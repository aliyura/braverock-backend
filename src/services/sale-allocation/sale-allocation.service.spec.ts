import { AllocationService } from './sale-allocation.service';

describe('AllocationService', () => {
  let service: AllocationService;

  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
