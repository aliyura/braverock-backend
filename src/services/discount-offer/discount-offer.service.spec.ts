import { DiscountOfferService } from './discount-offer.service';

describe('DiscountOfferService', () => {
  let service: DiscountOfferService;

  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
